{
  "name": "client",
  "version": "1.0.0",
  "description": "This is the client folder of the project. It contains the React application that serves as a SignalR client to receive notifications.",
  "main": "index.js",
  "scripts": {
    "start": "serve -s build",
    "build": "react-scripts build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@azure/msal-browser": "^3.23.0",
    "@microsoft/signalr": "^8.0.7",
    "client": "file:",
    "react-dom": "^18.3.1",
    "react-scripts": "^5.0.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@microsoft/signalr": "^8.0.7",
    "@types/react-dom": "^18.3.0",
    "react-scripts": "^5.0.0",
    "serve": "^14.2.3"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
